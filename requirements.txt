# Ambiente do PA1 — Segmentação de instâncias
# Python 3.14 (venv em .venv)
#
# Instalação:
#   uv venv --python 3.14 .venv
#   VIRTUAL_ENV=$PWD/.venv uv pip install -r requirements.txt \
#       --index-url https://download.pytorch.org/whl/cu128 \
#       --extra-index-url https://pypi.org/simple --index-strategy unsafe-best-match
#
# Para CPU apenas, troque o index-url por https://download.pytorch.org/whl/cpu

# --- Deep learning (CUDA 12.8) ---
torch==2.9.1+cu128
torchvision==0.24.1+cu128
timm==1.0.29              # encoders pré-treinados em ImageNet (permitido)

# --- Científico ---
numpy==2.5.3
scipy==1.18.1             # scipy.ndimage (permitido)
scikit-image==0.26.0      # skimage.segmentation, watershed (permitido)
scikit-learn==1.9.1       # sklearn.cluster: DBSCAN / MeanShift (permitido)
pandas==3.0.5
matplotlib==3.11.1

# --- Imagem / augmentation ---
albumentations==2.0.8     # (permitido)
opencv-python-headless==5.0.0
imageio==2.37.4
pillow==12.3.0

# --- Notebook ---
jupyterlab==4.6.3
ipykernel==7.3.0
tqdm==4.70.0
